#include "Booking.h"

int main(){
    // Booking b1 = Booking(Station("Mumbai"), Station("Delhi"),Date(12,5,2021),ACFirstClass::Type());
    // Booking b2 = Booking(Station("Kolkata"), Station("Delhi"),Date(5,3,2021),AC2Tier::Type());
    // Booking b3 = Booking(Station("Mumbai"), Station("Kolkata"),Date(17,3,2021), FirstClass::Type());
    // Booking b4 = Booking(Station("Mumbai"), Station("Delhi"), Date(23,3,2001), AC3Tier::Type());
    
    // vector<Booking*>::iterator it;
    // for(it = Booking::sBookings.begin(); it < Booking::sBookings.end(); ++it){
    //     cout << *(*it);
    // } 

    Booking::BookingUnitTest();

    return 0;
}